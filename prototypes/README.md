# Archived niche prototypes

The hood-cleaning, grease-trap, and client-portal applications in this directory are design and workflow references only. They use browser-local demo data and are excluded from the active workspace, release build, and Firebase deployment.

FieldLedger's maintained customer application is `apps/fieldledger`. New niche-specific work belongs in its configuration-driven workflow rather than in a separate app.
