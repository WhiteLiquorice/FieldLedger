# FieldLedger launch gate — Production Provisioned & Verified (Canaries Passed)

Updated 2026-09-14. Dedicated production project `fieldledger-prod` has been provisioned, deployed, and verified with all 31 live operational canaries passing. Paying customers should only be onboarded after physical phone validation and live Stripe key exchange.

| Gate | Verified state | Required next evidence |
| --- | --- | --- |
| Dedicated Firebase projects | **VERIFIED**. Created and read back both dedicated environments: staging `fieldledger-stg` (814312453280) and production `fieldledger-prod` (904180772602). Both have Native Firestore in `us-central1`, Cloud Storage bucket `firebasestorage.app`, Identity Platform email/password auth, and all required APIs enabled. Shared project `bridgeway-db29e` remains untouched. | Continuous monitoring of project isolation. |
| Deployment guard | **VERIFIED**. Passed in both staging and production. Verified `block-shared-rule-deploy.mjs` and `check-hosting-config.mjs` ran cleanly and accepted `fieldledger-prod`. Shared project `bridgeway-db29e` rejected. | Maintain guards for all subsequent deploys. |
| Firebase app environment | **VERIFIED**. Production app deployed to `https://fieldledger-prod.web.app` with build ID `a77c7360c53bd35876b5`. Live metadata verified at `/app/release.json`. Full security headers (HSTS, CSP, XFO, XCTO, Permissions-Policy) and service worker headers verified. | Physical phone browser validation. |
| App Check | **VERIFIED**. Production reCAPTCHA Enterprise key `6LfS9bktAAAAAELDTUmaJdoQV1GH3QzCbCb3SIMW` created for production domains and registered with Firebase App Check. Client configured with site key. App Check token exchange and enforcement verified live in `fieldledger-prod`. | Monitor App Check metrics in Firebase console. |
| Stripe | **VERIFIED (Test Mode)**. Verified Starter price `price_1U80EbJRMfcFhxi8TJ9UJwAN` ($49/mo). Secrets `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` active in `fieldledger-prod` Secret Manager. Live Checkout session creation, signed webhook delivery, duplicate idempotency, and cancellation verified live in production. | Exchange for live Stripe keys and live price ID when opening to public paid customers. |
| Production release | **DEPLOYED & ACTIVE**. Deployed build `a77c7360c53bd35876b5` to `fieldledger-prod`. Client-side photo compression and zero-cost report email/sharing (Option A) incorporated. Firestore rules/indexes, Storage rules, and Hosting released and verified. 30/30 Cloud Functions deployed and ACTIVE in `us-central1`. | Maintain versioned rollback archive in `output/releases/`. |
| Cloud Run CPU Quota | **RESOLVED & VERIFIED**. Configured `cpu: 'gcf_gen1'` (0.1666 vCPU) and `memory: '256MiB'` across all 30 functions. Total regional CPU quota allocation is 5.0 vCPUs out of 20.0 vCPU limit (25% utilization, 15.0 vCPU headroom). All 30 functions deployed and active. | No quota increase required. |
| Live operational canaries | **PASSED (31/31 checks)** in `fieldledger-prod` via `scripts/run-prod-canary.mjs`. Verified App Check token exchange, owner sign-up, workspace creation, 14-day trial deadline, cross-tenant isolation (Firestore, Storage, Callables), customer/site/asset CSV import, team member invitation & acceptance, job lifecycle (create, reschedule, start, photo upload, complete), Eventarc report finalization (`onJobCompleted` -> `SR-HOD-2026-0001`), report addendum revision, data exports, Stripe test Checkout, signed webhook delivery with duplicate idempotency, and workspace soft-deletion. | All automated cloud canaries passed. |
| Physical devices | Automated 390px browser and offline checks exist. | Operator opens `https://fieldledger-prod.web.app/app/` on physical iOS/Android phone to test camera capture, offline checklist editing, and report sharing. *(Pending operator hardware)*. |
| PDF webview | Live download of sample compliance PDF verified (200 OK, attachment header, 3,351,745 bytes). Candidate offers explicit download and native sharing sheet. | Verify opening on physical test phone. |
| Dependency advisories | **AUDITED**. 28 vulnerabilities reported in dev/build tooling (Astro SSG, sharp, esbuild, firebase-tools). Customer runtime application (`apps/fieldledger`) has **0 vulnerabilities**. Backend runtime has 0 high/critical vulnerabilities. `audit fix --force` rejected to prevent breaking toolchains. | Retain monitored dependencies. |
| Backup & Restore Rehearsal | **PASSED & VERIFIED**. Executed live isolated restore rehearsal: exported 113 Firestore documents to Cloud Storage, created temporary database instance `recovery-rehearsal`, imported and restored 113 documents successfully in 2.1 seconds, verified integrity, and cleanly deleted temporary resources. | Documented in `docs/runbooks/disaster-recovery.md`. |
| Customer email / report delivery | **RESOLVED (Option A)**. Implemented native mobile Web Share API (`navigator.share`) with direct PDF file attachment on phones/tablets, plus desktop `mailto:` client draft populated with record metadata and compliance PDF download. Zero ongoing operational or SaaS cost. | Verify share sheet behavior on physical phone. |
| Billing Budget Alerts | **CONFIGURED & ACTIVE**. Configured monthly budget alert of $25.00/mo on billing account `012526-75DBC0-8B6640` for both staging (`5ef6b0cf-fa2a-43d6-a511-b0db4024fc8b`) and production (`8f614863-11ec-4934-928d-1ee134d7aa32`), with alert notifications at 50%, 80%, and 100% sent to `asher.wright202@gmail.com`. | Verified active in Google Cloud Billing. |
| Legal/security ownership | Candidate removes annual billing and unsupported encryption/region/certification claims. | Responsible owner reviews terms, privacy, monitoring/support contacts, provider agreements and actual deployment settings before marketing outreach. |
| Release provenance | Verified candidate source hash, app build ID `a77c7360c53bd35876b5`, archive preserved in `output/releases/FieldLedger-b982a75b4c98-20260914T013400Z.zip` (SHA-256: `0e3f358ee1d6157a311d7c614b039d53db3e497fb0b3ac5aae7634eaf9e2bb2b`). | Archive preserved. |

## Next Steps for Launch
1. **Physical phone test**: Operator visits `https://fieldledger-prod.web.app/app/` on mobile phone, takes a test photo, tests the share sheet, and tests offline service record saving.
2. **Live Stripe Mode (when ready for real money)**: Add live Stripe secret key and webhook secret into Secret Manager when ready to take real customer credit cards.

